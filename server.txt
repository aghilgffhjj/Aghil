const express = require('express');
const app = express();
const port = 3000;

app.get('/', (req, res) => {
  res.send('سلام! سرور حسابداری شما با موفقیت فعال شد.');
});

app.listen(port, () => {
  console.log(`سرور در حال اجراست: http://localhost:${port}`);
});
